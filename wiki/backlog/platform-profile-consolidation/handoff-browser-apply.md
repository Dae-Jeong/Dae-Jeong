---
type: handoff
title: 브라우저 반영 실행 지시서 (Codex agent용)
description: 확정 문안을 채용 플랫폼에 반영하는 브라우저 작업 지시서. 문안 수정 권한 없음 — 반영과 검증만.
timestamp: 2026-08-10
status: superseded
tags: [handoff, browser, platform]
---

# 브라우저 반영 실행 지시서

> **Superseded execution record:** 이 파일의 붙여넣기 문안과 날짜는 당시 브라우저 작업 기록이다. 현재 입력 문안과 적용 체크리스트는 [Platform Profile Paste Package — Maker v1](2026-08-22-platform-paste-package.md)을 사용한다.

너의 임무는 **아래 문안을 채용 플랫폼에 그대로 반영하고, 저장을 검증하는 것**이다.

## 절대 규칙

1. **문안을 한 글자도 수정하지 않는다.** 오탈자로 보여도 고치지 않는다. 문제가 있으면 중단하고 보고한다.
2. **지정된 필드 외에는 아무것도 건드리지 않는다.** 다른 버튼·설정·삭제 조작 금지.
3. **"화면에 보인다 ≠ 저장됐다."** 저장 후 반드시 페이지를 새로고침하고 텍스트가 남아 있는지 확인한다.
4. 같은 조작이 3회 실패하면 중단하고 실패 상태를 보고한다. 우회 방법을 창작하지 않는다.
5. 브라우저 도구(paseo MCP) 외의 작업 금지 — 파일 수정 금지, 셸 명령 금지(이 파일을 읽는 것 제외).

## 브라우저 조작 방법

paseo MCP의 browser 도구를 쓴다. 탭은 `browser_list_tabs`로 찾는다.

**한글 텍스트 입력은 반드시 이 방법을 쓴다** (`browser_type`은 한글이 입력되지 않는다):

```js
// browser_evaluate 안에서:
const ta = /* 대상 textarea 찾기 */;
ta.focus();
ta.setSelectionRange(0, ta.value.length);   // 기존 내용 전체 선택
document.execCommand('insertText', false, NEW_TEXT);  // 교체
```

저장 버튼은 텍스트로 찾아 클릭한다. 저장 후:

```js
// browser_reload 후 browser_evaluate로:
document.body.innerText.includes('검증용 문자열')  // true여야 성공
```

---

## 패키지 1 — 리멤버 소개 교체 (두 탭 모두)

### 진입

1. `browser_list_tabs`에서 URL에 `rememberapp.co.kr`이 포함된 탭을 찾는다.
   - 편집 페이지 탭(`profile.rememberapp.co.kr/edit...`)이 있으면 그 탭을 쓴다.
   - 없으면 아무 리멤버 탭에서 `https://profile.rememberapp.co.kr/edit?internal_path=myprofile_menu#introduction`으로 이동한다.
2. 소개(자기소개) 편집 영역을 찾는다. **탭이 2개다: `커넥트용`, `채용 서비스용`.** 둘 다 교체해야 한다.

### 교체할 문안 (두 탭 동일)

아래 텍스트 전체를 그대로 넣는다. 문단 사이 빈 줄을 유지한다.

```
AI 제품을 만드는 일만 4년째 하고 있습니다. 모델을 만들다 기획으로, 다시 백엔드로 자리를 옮겼고 지금은 만드는 일과 무엇을 왜 만들지 정하는 일을 함께 맡고 있습니다.

백엔드를 택한 건 AI가 구현을 점점 더 많이 맡게 될수록, 보안과 안정성처럼 사람이 끝까지 책임지는 층의 무게가 커진다고 봤기 때문입니다. 지금 AI와 협업해 제품을 만들면서 그 생각은 더 확고해졌습니다.

"품질이 나쁘다"로는 고칠 대상을 정할 수 없습니다. AI 생성 품질을 자동 게이트와 실측 분포 대조, 사람 판정 3층으로 나눠 판정 가능하게 만들었습니다.
```

참고: 현재 들어가 있는 소개는 `AI 제품을 만들고, 무엇을 왜 만들지도 정하는 4년차 백엔드 엔지니어입니다`로 시작하는 3문단(약 306자)이다. 이것을 지우고 위 문안으로 교체한다.

### 순서

1. `커넥트용` 탭 선택 → textarea 내용 교체 → 저장 버튼 클릭
2. `채용 서비스용` 탭 선택 → 동일하게 교체 → 저장
3. 페이지 새로고침(browser_reload) → 3~5초 대기
4. 검증: 본문에 `AI 제품을 만드는 일만 4년째` 포함 **그리고** `대체될` 미포함 **그리고** `4년차 백엔드 엔지니어입니다` 미포함
5. 두 탭 각각에 대해 4를 확인한다 (탭 전환 후 확인)

### 보고 형식

작업 종료 시 아래 형식으로 최종 보고한다:

```
[패키지 1 결과]
- 커넥트용: 교체 성공/실패 · reload 검증 통과/실패
- 채용 서비스용: 교체 성공/실패 · reload 검증 통과/실패
- 이상 징후: (있으면 서술, 없으면 "없음")
- 시도 중 실패한 조작: (있으면 무엇을 몇 회)
```

---

## 패키지 1 결과 (2026-08-11)

✅ 완료 — 커넥트용·채용 서비스용 모두 교체·reload 검증 통과. 부모 agent 교차 검증 완료 (302자·대체 표현 0·구버전 0).

---

## 패키지 2 — 리멤버 경력 설명 교체 (5건)

같은 리멤버 프로필 편집 화면에서 **경력** 섹션의 회사별 설명(주요 업무/성과) textarea를 교체한다.

규칙 추가:
- **직함·기간·회사명 필드는 절대 건드리지 않는다.** 설명 textarea만 교체한다.
- 입력 후 값이 잘렸다면(maxLength로 뒷부분 소실) **저장하지 말고 중단·보고**한다.
- 회사 항목을 못 찾으면 그 건은 건너뛰고 보고에 남긴다.

### 2-1. 메디솔브에이아이

```
[Thready] 속도를 우선해 빠르게 검증된 초기 제품의 backend를 인계받아 FastAPI로 재구축했습니다. 인계 시점의 문제는 도메인 간 의존성이었습니다. 회원 로직을 수정하면 AI 생성이 중단되는 식으로 변경이 전파됐고, QA 티켓을 닫아도 같은 영역에서 다른 형태로 재발하는 패턴이 반복됐습니다.

"돌아가는 기능을 왜 다시 만드나"라는 반대가 있었습니다. 부분 수정으로는 의존성 구조가 남아 문제가 계속 누적된다는 점, AI 모듈 확장이 예정돼 있다는 점, 하네스를 먼저 세우면 이관이 오히려 빠르다는 점으로 설득했습니다. 서비스 규모가 아직 작아 재구축 비용이 가장 낮은 구간이기도 했습니다. FE(Next.js)는 유지하고 backend만 분리 교체하는 범위로 정했습니다.

실행은 디자인 패턴과 컴포넌트 설계, 인프라 하네스를 먼저 세운 뒤 그 규칙 위에서 AI와 협업하는 순서로 했습니다. 파악부터 기능 정의, 재구축까지 작업 시간 기준 36시간이 걸렸습니다. cutover 전후 Jira 집계에서 QA 티켓의 resolve 대비 reopen 비율은 37%에서 11%로 감소했고, 현재 운영 지표는 30일 기준 월 수만 건 규모에서 HTTP 5xx 0.3% 수준입니다.

[Centurion] 피부과 운영 CRM인 Centurion에서 order·product·inventory API와 비동기 worker 흐름을 구축했습니다. 이 설계는 장애를 겪고 대응한 것이 아니라 제품 시작 시점의 예방책이었습니다. 직전 회사에서 결제 실패를 수습하며 롤백 순서와 환불 시 티켓 제거 시점처럼 중간 단계 실패가 남기는 불일치를 직접 처리한 경험이 있었습니다. 주문·결제처럼 실패 가능한 작업은 처음부터 API 경계 밖 worker로 두었고, 재고 차감같이 실패 시 정합성이 깨지는 작업에는 retry를 붙였습니다. 이후 메시지 처리 계층을 Celery에서 TaskIQ로 전환하며 알림 도메인을 함께 정리했습니다.

[조직 표준] 백엔드 인원 대비 담당 제품이 많은 구조에서는 제품 간 이동 비용이 병목이 됩니다. 조직 표준 FastAPI 템플릿을 설계·구축했습니다. layered architecture와 의존성 주입, 응답·예외 규약, 타입 안정성, ADR과 runbook을 담았고 계층형 agent context, Hub-and-Spoke 문서 라우팅, 도메인·셋업 자동화 스킬을 함께 내장했습니다. 신규 backend가 같은 구조에서 출발하므로 제품 정책만 파악하면 대응할 수 있고, 온보딩이나 FE 엔지니어도 같은 패턴 위에서 backend 로직을 구현할 수 있습니다.

Backend Engineer로 위 시스템을 담당하면서 기업부설연구소장과 Tech Lead, PO를 병행하고 있습니다.
```

### 2-2. 더데이랩스

```
현 MediSolve AI 대표와 프리랜서로 협업을 시작했습니다. Centurion의 backend 저장소를 처음 만들면서 프로젝트 기본 구조와 Config 설계, SSH 터널링 lifecycle, 환경·실행 문서, ISSUE/PR 템플릿을 함께 넣었습니다. 처음 꾸려지는 개발팀이라 코드보다 기준이 먼저 필요하다고 보았습니다. 이후 창업 시점에 합류해 같은 제품을 이어 맡았습니다.
```

### 2-3. 메멘토에이아이

```
피부과 통합 관리 시스템의 예약·결제 backend를 담당했습니다. Stripe 선결제를 도입했고 Hongkong API로 다국가 결제와 구독·환불의 트랜잭션 상태를 처리했습니다.

결제는 중간 단계에서 실패할 때 상태가 어긋난 채 남는 것이 문제였습니다. 선결제 예약이 실패했을 때의 롤백, 환불 처리 순서, 환불 시 티켓을 제거하는 시점, 전액 마일리지 결제 건의 환불처럼 실패 지점마다 남는 불일치를 하나씩 잡았습니다. 알림톡은 발송 실패 시 3회까지 자동 재시도하도록 했습니다.

인턴으로 1개월간 병원 내부 일정 관리 어드민을 0에서 구축한 뒤 정규직으로 전환했고, 회사 폐업으로 재직이 종료됐습니다.
```

### 2-4. 스튜디오랩

```
상품 이미지를 Vision AI로 분석해 상세페이지를 자동 생성하는 SellerCanvas에서 일했습니다. AI Engineer로 합류해 의류 이미지 분석 모델을 개발했고(2021.12~2022.09), PM으로 옮겨 제품이 운영되는 시스템을 기획·구축했으며(2022.10~2023.09), 마지막 구간은 Backend Engineer였습니다(2023.10~2023.12).

PM 시기에는 패션 대기업 브랜드 POC를 맡아 비즈니스 요구사항을 기술 스펙으로 변환했습니다. Backend 구간에는 Node.js 레거시를 NestJS로 옮기며 NestMiddleware와 ExceptionFilter 기반 예외 처리 구조를 세웠고, JWT 인증과 Controller→Service→DAO 흐름을 담은 팀 개발 템플릿을 만들었습니다. 지금 조직 표준 템플릿을 설계하는 일의 첫 형태였습니다.

SellerCanvas는 CES 2024 AI 부문 Best of Innovation을 수상했습니다.
```

### 2-5. 아이즈솔

```
유아 안면 인식 기반 비접촉 자동 출결 시스템 Kidsly를 개발했습니다. 인식 모델 개발과 학습 데이터 수집·관리, 영상 데이터 전처리 파이프라인을 담당했고 C# 기반 백엔드도 함께 다뤘습니다. 인턴이었지만 기능 요구사항 정의와 일정·태스크 관리까지 맡았고, 이 시기에 이미 만드는 일과 정하는 일을 병행했습니다.
```

### 검증

각 건 저장 후 reload → 해당 회사 설명에 아래 문자열 포함 확인:
2-1 `돌아가는 기능을 왜 다시 만드나` / 2-2 `코드보다 기준이 먼저` / 2-3 `실패 지점마다 남는 불일치` / 2-4 `제품이 운영되는 시스템` / 2-5 `만드는 일과 정하는 일`

### 보고 형식

```
[패키지 2 결과]
- 메디솔브에이아이: 성공/실패/건너뜀 · 검증
- 더데이랩스: …
- 메멘토에이아이: …
- 스튜디오랩: …
- 아이즈솔: …
- 이상 징후:
```

---

## 패키지 3 — 그룹바이 정찰 (읽기 전용, 수정 금지)

`https://groupby.kr/scouts/my-profile` 탭에서 **아무것도 수정하지 말고** 다음을 그대로 보고한다:

1. 자기소개 필드의 **전문** (글자 하나 빠짐없이)
2. 경력 섹션 각 회사 항목의 설명 텍스트 전문
3. 각 필드가 textarea인지 contenteditable인지, maxLength가 있는지

이 정찰 결과로 부모 agent가 패키지 4(그룹바이 교체)를 작성한다.

---

## 패키지 2·3 결과 (2026-08-11)

- 패키지 2: 메디솔브 ✅ · 더데이랩스 ✅ · **메멘토 실패 → 부모 agent가 직접 복구** (설명 341자 + 비어 있던 직책 `Backend Engineer` 복원, reload 검증 통과)
- **스튜디오랩·아이즈솔은 교체하지 않기로 확정** — 리멤버에는 직무별 기간으로 나뉜 서비스별 설명이 이미 있고(스튜디오랩 3건), canonical 한 덩어리로 덮으면 구조가 오히려 나빠진다. 건너뛴 판단이 맞았다.
- 패키지 3 정찰 완료 → 아래 패키지 4의 재료가 됐다.
- **그룹바이 경력 설명은 교체하지 않는다** — 현재 내용(마이크로서비스·TaskIQ·SSO·NEXUS 상세)이 canonical보다 기술 상세가 풍부하다. 자기소개만 교체한다.

---

## 패키지 4 — 그룹바이 자기소개 교체

`https://groupby.kr/scouts/my-profile` 탭. **자기소개 필드 하나만 교체한다. 경력 설명은 절대 건드리지 않는다.**

### 진입

자기소개는 보기 모드에서 SPAN(읽기 전용)이다. 자기소개 영역 근처의 **편집(연필/수정) 버튼을 클릭**해 편집기를 연다. 편집기가 textarea면 execCommand 방식, contenteditable이면 전체 선택 후 insertText.

### 교체할 문안 (전문 — 목록까지 포함해 이 전체로 교체)

```
AI 제품을 만드는 일만 4년째 하고 있습니다. 모델을 만들다 기획으로, 다시 백엔드로 자리를 옮겼고 지금은 만드는 일과 무엇을 왜 만들지 정하는 일을 함께 맡고 있습니다.

백엔드를 택한 건 AI가 구현을 점점 더 많이 맡게 될수록, 보안과 안정성처럼 사람이 끝까지 책임지는 층의 무게가 커진다고 봤기 때문입니다. 지금 AI와 협업해 제품을 만들면서 그 생각은 더 확고해졌습니다.

"품질이 나쁘다"로는 고칠 대상을 정할 수 없습니다. AI 생성 품질을 자동 게이트와 실측 분포 대조, 사람 판정 3층으로 나눠 판정 가능하게 만들었습니다.

■ 수상·자격·특허

[수상] 2023.12 CES 2024 Best of Innovation (AI 부문) - 대상 제품 참여
[수상] 2021.02 전국 SW 중심대학 공동 해커톤 우수상
[수상] 2020.10 SW 창업 아이디어 경진대회 아이디어 부문 2등
[수상] 2019.11 SW 창업 아이디어 경진대회 개발 및 구현 부문 2등
[자격증] 2021.09 데이터 분석 준전문가 (ADsP)
[특허] 페이지 출력 방법 - 등록 10-2898273
```

### 검증 (저장 → reload 후)

1. `AI 제품을 만드는 일만 4년째` 포함
2. `[특허] 페이지 출력 방법 - 등록 10-2898273` 포함 (목록 보존 확인)
3. `4년차 백엔드 엔지니어입니다` 미포함 · `대체될` 미포함

### 보고 형식

```
[패키지 4 결과]
- 자기소개: 교체 성공/실패 · reload 검증 1/2/3 각각 통과 여부
- 이상 징후:
```

---

## 패키지 4 결과 (2026-08-11)

✅ 완료 — 그룹바이 자기소개 v5.1 교체. Luna reload 검증 3항목 + 부모 교차 검증 5항목(v5.1·특허 목록 보존·구버전 0·적대 표현 0·경력 설명 유지) 전부 통과.

---

## 패키지 5 — 링크드인 경력 설명 교체

⚠️ **링크드인 특수 제약 — 반드시 지켜라:**
1. **browser_evaluate가 CSP로 차단된다.** 사용 금지. `browser_snapshot`(filter: "interactive", maxChars 3000 이하) + `browser_fill` + `browser_click`만 쓴다.
2. `browser_type` 한글 미지원 — 텍스트 입력은 **browser_fill만** 쓴다.
3. **회사명·직함·기간 필드 절대 금지.** 링크드인 typeahead 필드는 건드리면 값이 깨진다(실측됨). **설명(description) textarea만** fill한다.
4. 검증은 reload 후 snapshot 텍스트에 검증 문자열이 보이는지로 한다.

### 진입

`https://www.linkedin.com/in/marinkim-dev/details/experience/` 로 이동. 각 경력 항목의 수정(연필) 버튼 클릭 → 모달의 설명 textarea를 fill → 저장.

### 항목별 문안 (패키지 2의 문안을 그대로 재사용)

| 링크드인 항목 | 넣을 문안 |
| --- | --- |
| MediSolve AI (메디솔브에이아이) | 패키지 2-1 전문 |
| 데이뷰네트웍스 또는 더데이랩스 | 패키지 2-2 전문 |
| Memento AI 정규직 항목 | 패키지 2-3 전문 |
| Memento AI 인턴 항목 (별도로 있는 경우만) | 아래 별도 문안 |
| STUDIO LAB (스튜디오랩) | 패키지 2-4 전문 |
| 아이즈솔 | 패키지 2-5 전문 |

Memento 인턴 항목용 (항목이 분리돼 있을 때만):

```
병원 내부 직원 일정 관리 어드민 Check를 기획부터 API·DB 설계, 구현까지 0에서 구축했습니다. 회원 도메인(이메일/비밀번호 인증, JWT 세션 관리)을 설계했고, 1개월 후 정규직으로 전환했습니다.
```

### 검증 문자열 (reload 후 snapshot에 포함 확인)

MediSolve `돌아가는 기능을 왜 다시 만드나` / 더데이랩스 `코드보다 기준이 먼저` / Memento `실패 지점마다 남는 불일치` / STUDIO LAB `제품이 운영되는 시스템` / 아이즈솔 `만드는 일과 정하는 일`

### 보고 형식

```
[패키지 5 결과]
- 항목별: 성공/실패/건너뜀 · 검증
- 이상 징후:
```

---

## 패키지 5 결과 + 세션 마감 (2026-08-11)

✅ **패키지 5 완료** — 링크드인 경력 설명 6건 전부 교체·검증 (MediSolve·더데이랩스(구 데이뷰네트웍스 항목)·Memento 정규직·Memento 인턴·STUDIO LAB·아이즈솔). Luna가 CSP 제약 하에서 snapshot+fill로 수행.

⚠️ **남은 이상 1건**: 아이즈솔 기간이 링크드인에서 `2020.08–2021.08 (1년 1개월)`로 표시됨.
canonical은 `2020.08–2021.06 (11개월)`. Luna는 기간 필드를 건드리지 않았으므로 기존 값이다.
**수동 확인·수정 필요** (경력 편집 → 종료일 2021년 6월).

❌ **링크드인 영어 프로필 신설 — 자동화 불가 판정**. 외국어 프로필 생성 폼에서:
- `browser_fill`: DOM에는 들어가나 폼 상태에 등록되지 않음
- `browser_type`: 커서 위치에 삽입돼 fill 잔여물과 섞임
- `Meta+a` 전체 선택: 이 폼에서 동작하지 않음
3회 초과 실패로 중단 규칙에 따라 **저장 없이 폐기**했다 (프로필 무변경 확인).
→ 수동 생성 필요. 준비된 값: 이름 `Daejeong` / 성 `Kim` / 헤드라인
`Backend Engineer · PO | Building AI products — and deciding what to build and why | Head of Corporate R&D Center | PM of a CES 2024 Best of Innovation product`
/ About은 resume-view.tsx EN Summary 참조.

---

## 패키지 6 — 소개 v5.2 재전파 (번역투 제거)

소개 문안이 v5.2로 개정됐다. 이미 나간 3개 플랫폼을 재전파한다.
방법은 각 플랫폼에서 이전에 성공한 방식 그대로다 (리멤버·그룹바이·로켓펀치 모두 execCommand 동작).

### 6-1. 리멤버 소개 — 커넥트용·채용 서비스용 두 탭 모두 교체

```
AI 제품만 4년째 만들고 있습니다. 모델을 만들다 기획을 거쳐 백엔드로 왔고, 지금은 백엔드를 만들면서 뭘 만들지 정하는 역할도 같이 맡고 있습니다.

백엔드를 택한 데는 이유가 있습니다. AI가 구현을 점점 더 많이 맡을수록, 보안과 안정성처럼 사람이 끝까지 책임져야 하는 층이 더 무거워진다고 봤습니다. 매일 AI와 같이 제품을 만드는 지금, 그 판단이 틀리지 않았다고 느낍니다.

"품질이 나쁘다"는 말로는 뭘 고쳐야 할지 알 수 없습니다. 그래서 AI가 만든 결과물의 품질을 자동 게이트와 실측 분포 대조, 사람 판정 세 층으로 나눠 판정할 수 있게 만들었습니다.
```

### 6-2. 그룹바이 자기소개 — 위 3문단 + 기존 `■ 수상·자격·특허` 목록을 그대로 이어붙인 전문으로 교체

(목록 부분은 현재 필드에 있는 것을 그대로 보존한다 — `[수상]`~`[특허]` 6줄)

### 6-3. 로켓펀치 소개 — 300자 제한, 아래 축약본으로 교체

```
AI 제품만 4년째 만들고 있습니다. 백엔드를 만들면서 뭘 만들지 정하는 역할도 같이 맡고 있습니다.

백엔드를 택한 데는 이유가 있습니다. AI가 구현을 점점 더 많이 맡을수록, 보안과 안정성처럼 사람이 끝까지 책임져야 하는 층이 더 무거워진다고 봤습니다.

"품질이 나쁘다"는 말로는 뭘 고쳐야 할지 알 수 없습니다. 그래서 AI가 만든 결과물의 품질을 자동 게이트, 실측 분포 대조, 사람 판정 세 층으로 나눠 판정할 수 있게 만들었습니다.
```

### 검증 (각 플랫폼 저장 → reload 후)

1. `AI 제품만 4년째 만들고 있습니다` 포함
2. `만드는 일과` 미포함 · `무엇을 왜 만들지` 미포함 (구버전 제거 확인)
3. 그룹바이는 `[특허] 페이지 출력 방법` 포함 (목록 보존)

### 보고 형식

```
[패키지 6 결과]
- 리멤버 커넥트용/채용 서비스용: …
- 그룹바이: …
- 로켓펀치: …
- 이상 징후:
```

---

## 패키지 6 결과 (2026-08-11)

✅ 3곳 전부 완료 — 리멤버 2탭·그룹바이(목록 보존)·로켓펀치(249자). Luna 검증 + 부모 교차 검증(리멤버·그룹바이) 통과. 구버전 문구 잔존 0.
v5.2 소급: 웹 이력서 헤더·Summary, identity.md Canonical One-Line("무엇을 왜" 제거), 링크드인 헤드라인은 붙여넣기 패키지 §1.5로.

---

## 패키지 7 — 소개 v6 + 메디솔브 경력 v2 전파

문안 canonical이 v6(관점 선언)·L2 v2(AX·Agent)로 개정됐다. 3개 플랫폼을 재전파한다.
각 플랫폼에서 이전에 성공한 방식 그대로 (execCommand → 저장 → reload 검증).

### 7-1. 리멤버 소개 — 커넥트용·채용 서비스용 두 탭 모두

```
LLM 기능을 제품에 붙이는 일보다, AI의 출력을 판정하고 통제해서 사람이 믿고 쓸 수 있는 제품으로 만드는 일에 관심이 있습니다.

그래서 LLM 백엔드, Agent Workflow, Evaluation을 중심으로 문제 정의부터 설계, 구현, 평가, 운영까지 제품의 전체 과정을 다룹니다.

"품질이 나쁘다"는 말로는 뭘 고쳐야 할지 알 수 없습니다. AI가 만든 결과물의 품질을 자동 게이트, 실측 분포 대조, 사람 판정 세 층으로 나눠 판정할 수 있게 만들었습니다.

제가 계속 붙들고 있는 질문은 하나입니다. "AI가 구현을 점점 더 많이 맡을수록, 사람이 끝까지 책임져야 하는 층을 어떻게 지킬 것인가?"

사람들에게 필요한 AI 제품을 4년째 만들어온 엔지니어로서, 이 질문의 답을 의료 AI 플랫폼에서 제품으로 만들어가고 있습니다.
```

### 7-2. 리멤버 메디솔브에이아이 경력 설명 — wanted-paste-package.md §4의 설명 전문과 동일하게 교체

(같은 디렉토리의 `wanted-paste-package.md` §4 설명 코드블록을 그대로 사용. [Thready]~[조직 표준·AX] 6단락 + 마지막 줄)

### 7-3. 그룹바이 자기소개 — 7-1 문안 + 기존 `■ 수상·자격·특허` 6줄을 그대로 이어붙인 전문으로 교체

### 7-4. 로켓펀치 소개 — 300자 제한, 아래 축약본

```
LLM 기능을 붙이는 일보다, AI의 출력을 판정하고 통제해서 사람이 믿고 쓸 수 있는 제품으로 만드는 일에 관심이 있습니다.

"품질이 나쁘다"는 말로는 뭘 고쳐야 할지 알 수 없습니다. AI가 만든 결과물의 품질을 자동 게이트, 실측 분포 대조, 사람 판정 세 층으로 나눠 판정할 수 있게 만들었습니다.

제 질문은 하나입니다. "AI가 구현을 더 많이 맡을수록, 사람이 끝까지 책임질 층을 어떻게 지킬 것인가?" 사람들에게 필요한 AI 제품을 4년째 만들며 답을 찾고 있습니다.
```

### 7-5. 로켓펀치 메디솔브에이아이 경력 설명 — wanted-paste-package.md §4의 설명 전문으로 교체

### 검증 (각 저장 → reload)

1. 소개: `LLM 기능을` 포함 · `AI 제품만 4년째` 미포함 (구버전 제거)
2. 메디솔브 설명: `[Agent Workflow]` 포함 · `planner와 writer` 포함
3. 그룹바이: `[특허] 페이지 출력 방법` 포함 (목록 보존)

### 보고 형식

```
[패키지 7 결과]
- 리멤버 소개 2탭 / 리멤버 메디솔브: …
- 그룹바이 소개: …
- 로켓펀치 소개 / 로켓펀치 메디솔브: …
- 이상 징후:
```

---

## 패키지 7 결과 (2026-08-11)

✅ 3플랫폼 전부 완료 — Luna reload 검증 + 부모 교차 검증(리멤버·그룹바이) 통과.
소개 v6(관점 선언·관통 질문)·메디솔브 L2 v2([LLM Evaluation]·[Agent Workflow]) 정렬 상태:
리멤버 ✅ · 그룹바이 ✅(목록·경력 보존) · 로켓펀치 ✅ · 링크드인 = 붙여넣기 패키지 §1 대기 · 원티드 = 패키지 사용자 반영 중.

---

## 패키지 8 — 스튜디오랩 PM 보강판 + 특허 인과 전파 (2026-08-12)

**상태**: ⏸️ 대기 — Paseo 브라우저 재시작으로 로켓펀치 세션 만료(로그인 화면).
사용자가 Paseo 브라우저에서 로켓펀치 로그인하면 즉시 실행 가능.

### 8-1. 로켓펀치 — 주식회사스튜디오랩 경력 설명 교체

`wanted-paste-package.md` §6의 설명 전문(특허 인과 + PM 중 직접 제작 포함)으로 교체.
로켓펀치는 execCommand 방식이 동작하고, 저장 버튼은 React fiber `__reactProps$*.onClick` 우회가 필요했다.

### 8-2. 검증 (저장 → reload)

1. 스튜디오랩 설명에 `「페이지 출력 방법」 특허 출원으로 이어져` 포함
2. `PM으로 있으면서도 필요하면 직접 만들었습니다` 포함
3. 구버전 `패션 대기업 브랜드 POC를 맡아 비즈니스 요구사항을` 단독 단락 미포함

### 결과 기록

- 로켓펀치 스튜디오랩: …

---

## 패키지 9 — bullet 하이브리드 전면 전환 + C# 오기 정정 (2026-08-12)

**상태**: ⏸️ 로그인 대기 (로켓펀치·리멤버·그룹바이 세션 만료)

### 9-0. ⚠️ 사실 오류 정정 — 최우선

live 프로필 3곳(리멤버·그룹바이·로켓펀치)의 **아이즈솔 설명에 "C# 기반 백엔드"가 게시돼 있다.**
2026-08-12 user-refuted — 아이즈솔에서 C#은 하지 않았고 백엔드는 Python(FastAPI)다.
사실과 다른 내용이므로 다른 무엇보다 먼저 교체한다.

### 9-1. 경력 설명 전 건을 bullet 하이브리드로 교체

canonical: `dual-track-copy-draft.md` 「경력 설명 — bullet 하이브리드 canonical」
플랫폼 파생: 원티드 = `wanted-paste-package.md` §4~§7 / 링크드인 = `linkedin-paste-package.md` §2

대상: 메디솔브 · 더데이랩스 · 메멘토(정규직/인턴) · 스튜디오랩 · 아이즈솔

### 9-2. 검증 (각 저장 → reload)

1. 아이즈솔에 `Python·FastAPI` 포함 · `C#` 미포함
2. 각 경력 설명이 `[` 소제목으로 시작하고 `- 판단:` 줄을 포함
3. 스튜디오랩에 `「페이지 출력 방법」 특허 출원으로 이어져` 포함

### 결과 기록

- 리멤버 / 그룹바이 / 로켓펀치: …

---

## ⚠️ 기록 정정 (2026-08-13 실측)

**이 문서의 반영 이력이 실제 게시 상태와 어긋난 항목이 확인됐다.**
[동기화 매트릭스](2026-08-13-sync-matrix.md)가 실측 기준의 단독 소유자다.

- 링크드인 About을 "v5.2 · 교체 대기"로 적어뒀으나 **실제로는 v6이 반영돼 있다**
- 링크드인 URL이 `대정-김-057b2a227` → `marinkim-dev`로 변경됨

**규칙**: 착수 전 반드시 브라우저로 실측한다. 반영 이력만 믿고 다음 작업을 설계하지 않는다.
