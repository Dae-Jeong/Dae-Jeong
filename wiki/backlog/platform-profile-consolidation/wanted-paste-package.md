---
type: handoff
title: 원티드 붙여넣기 패키지 (v6 기준)
description: 원티드 텍스트 필드는 자동 입력이 저장되지 않는다. 아래를 순서대로 복사→붙여넣기한다.
timestamp: 2026-08-11
status: ready
tags: [wanted, paste, copy]
---

# 원티드 붙여넣기 패키지 — v6 (관점 선언) 기준

**이력서 주소**: https://www.wanted.co.kr/cv/fF1dU0gKAAsDCw4BRE4=

클릭으로 되는 것(재직 형태·졸업·스킬 9건·중복 삭제)은 이미 반영돼 있다.
**각 필드: 복사 → 붙여넣기 → 다른 곳 클릭(blur) → 새로고침으로 저장 확인.**
원티드는 화면 표시와 실제 저장이 다른 경우가 실측됐으니 새로고침 확인을 건너뛰지 않는다.

---

## 1. 간단 소개

```
LLM 기능을 제품에 붙이는 일보다, AI의 출력을 판정하고 통제해서 사람이 믿고 쓸 수 있는 제품으로 만드는 일에 관심이 있습니다.

그래서 LLM 백엔드, Agent Workflow, Evaluation을 중심으로 문제 정의부터 설계, 구현, 평가, 운영까지 제품의 전체 과정을 다룹니다.

"품질이 나쁘다"는 말로는 뭘 고쳐야 할지 알 수 없습니다. AI가 만든 결과물의 품질을 자동 게이트, 실측 분포 대조, 사람 판정 세 층으로 나눠 판정할 수 있게 만들었습니다.

제가 계속 붙들고 있는 질문은 하나입니다. "AI가 구현을 점점 더 많이 맡을수록, 사람이 끝까지 책임져야 하는 층을 어떻게 지킬 것인가?"

사람들에게 필요한 AI 제품을 4년째 만들어온 엔지니어로서, 이 질문의 답을 의료 AI 플랫폼에서 제품으로 만들어가고 있습니다.
```

## 2. AI 활용 경험 (AX) — 항목당 50자 제한

한 항목만 된다면 1번을, 여러 항목이 추가되면 순서대로:

```
LLM 생성 품질 평가 하네스 설계 (자동 게이트·실측 분포·사람 판정)
```

```
planner-writer agent 파이프라인 설계·구현
```

```
AI 코딩 에이전트 협업 워크플로우 팀 도입
```

```
agent context 내장 조직 표준 템플릿 구축
```

## 3. 학력 — 학교명 오타 정정

학교명 필드: `우송대힉교` → 아래로 교체

```
우송대학교
```

## 4. 메디솔브에이아이 주식회사

| 필드 | 값 |
| --- | --- |
| 직무 | `백엔드 개발자` |
| 직책 | `기업부설연구소장` |
| 주요 성과(제목) | `AI 제품 backend 구축·운영과 LLM 평가·Agent Workflow 설계` |

설명 (전문):

```
[Thready] 속도를 우선해 빠르게 검증된 초기 제품의 backend를 인계받아 FastAPI로 재구축했습니다. 인계 시점의 문제는 도메인 간 의존성이었습니다. 회원 로직을 수정하면 AI 생성이 중단되는 식으로 변경이 전파됐고, QA 티켓을 닫아도 같은 영역에서 다른 형태로 재발하는 패턴이 반복됐습니다.

"돌아가는 기능을 왜 다시 만드나"라는 반대가 있었습니다. 부분 수정으로는 의존성 구조가 남아 문제가 계속 누적된다는 점, AI 모듈 확장이 예정돼 있다는 점, 하네스를 먼저 세우면 이관이 오히려 빠르다는 점으로 설득했습니다. 디자인 패턴과 인프라 하네스를 먼저 세운 뒤 그 규칙 위에서 AI 코딩 에이전트와 협업해, 파악부터 재구축까지 작업 시간 기준 36시간에 마쳤습니다. cutover 전후 Jira 집계에서 QA 티켓의 resolve 대비 reopen 비율은 37%에서 11%로 감소했고, 현재 30일 기준 월 수만 건 규모에서 HTTP 5xx 0.3% 수준으로 운영하고 있습니다.

[LLM Evaluation] "품질이 나쁘다"는 말로는 재생성 기준을 정할 수 없어, 생성 품질을 판정 가능한 대상으로 만들었습니다. 틀린 출력을 거르는 자동 게이트 12종, 직접 수집한 실측 코퍼스(n=19에서 4,039까지 확대)와의 분포 대조, 자동화가 닿지 않는 층의 사람 판정 — 세 층으로 평가 체계를 나눴습니다. 반증된 프롬프트 규칙은 기록으로 남겨 같은 시도가 반복되지 않게 했습니다.

[Agent Workflow] 평가 하네스만 만든 것이 아니라, 하네스로 검증할 agent 구조 자체를 함께 설계·구현했습니다. 생성 파이프라인을 planner와 writer 역할로 나눴고, 대표적으로 writer 영역을 설계하고 구현했습니다. 유형 분기 판정을 writer에 뒀을 때 18건 전부 발동하지 않았는데, writer는 원본이 어떤 유형인지 알 수 없는 자리였기 때문입니다. 판정을 planner로 옮기자 유형별로 정확히 갈렸습니다 — 판단을 어느 역할에 둘 것인가가 agent 설계의 핵심이라는 것을 설계와 실측 양쪽에서 확인했습니다.

[Centurion] 피부과 운영 CRM인 Centurion에서 order·product·inventory API와 비동기 worker 흐름을 구축했습니다. 직전 회사에서 결제 실패의 불일치를 직접 수습한 경험을 바탕으로, 실패 가능한 작업은 제품 시작 시점부터 API 경계 밖 worker로 두고 재고 차감에는 retry를 붙였습니다. 이후 메시지 계층을 Celery에서 TaskIQ로 전환하며 알림 도메인을 정리했고, 상담 현장을 실시간으로 돕는 AI 에이전트(WebSocket 기반 STT·LLM 파이프라인) 구축에는 공동 주 기여로 참여하고 있습니다.

[조직 표준·AX] 백엔드 인원 대비 담당 제품이 많은 구조에서는 제품 간 이동 비용이 병목이 됩니다. 조직 표준 FastAPI 템플릿에 layered architecture, 의존성 주입, 응답·예외 규약, ADR·runbook을 담고, 계층형 agent context와 도메인·셋업 자동화 스킬을 내장했습니다. 사람과 코딩 에이전트가 같은 규칙 위에서 일하는 개발 워크플로우를 팀에 도입한 것으로, 신규 backend가 같은 구조에서 출발하므로 제품 정책만 파악하면 누구든 대응할 수 있습니다.

Backend Engineer로 위 시스템을 담당하면서 기업부설연구소장과 Tech Lead, PO를 병행하고 있습니다.
```

## 5. 주식회사 메멘토에이아이

| 필드 | 값 |
| --- | --- |
| 직무 | `백엔드 개발자` |
| 주요 성과(제목) | `예약·결제 backend 안정화` |

```
피부과 통합 관리 시스템의 예약·결제 backend를 담당했습니다. Stripe 선결제를 도입했고 Hongkong API로 다국가 결제와 구독·환불의 트랜잭션 상태를 처리했습니다.

결제는 중간 단계에서 실패할 때 상태가 어긋난 채 남는 것이 문제였습니다. 선결제 예약 실패 시의 롤백, 환불 처리 순서, 환불 시 티켓 제거 시점, 전액 마일리지 결제 건의 환불처럼 실패 지점마다 남는 불일치를 하나씩 잡았습니다. 알림톡은 발송 실패 시 3회까지 자동 재시도하도록 했습니다.

인턴으로 1개월간 병원 내부 일정 관리 어드민을 0에서 구축한 뒤 정규직으로 전환했고, 회사 폐업으로 재직이 종료됐습니다.
```

## 6. 주식회사스튜디오랩

| 필드 | 값 |
| --- | --- |
| 직무 | `프로덕트 매니저` |
| 주요 성과(제목) | `SellerCanvas 제품 시스템 기획·구축` |

```
상품 이미지를 Vision AI로 분석해 상세페이지를 자동 생성하는 SellerCanvas에서 일했습니다. AI Engineer로 합류해 의류 이미지 분석 모델을 개발했고(2021.12~2022.09), PM으로 옮겨 제품이 운영되는 시스템을 기획·구축했으며(2022.10~2023.09), 마지막 구간은 Backend Engineer였습니다(2023.10~2023.12).

PM 시기에는 패션 대기업 브랜드 POC를 맡아 비즈니스 요구사항을 기술 스펙으로 변환했습니다. Backend 구간에는 Node.js 레거시를 NestJS로 옮기며 예외 처리 구조를 세웠고, JWT 인증과 Controller→Service→DAO 흐름을 담은 팀 개발 템플릿을 만들었습니다. 지금 조직 표준 템플릿을 설계하는 일의 첫 형태였습니다.

SellerCanvas는 CES 2024 AI 부문 Best of Innovation을 수상했습니다.
```

## 7. 주식회사 아이즈솔

| 필드 | 값 |
| --- | --- |
| 직무 | `백엔드 개발자` |
| 주요 성과(제목) | `Vision AI 기반 비대면 출결 서비스 backend` |

```
유아 안면 인식 기반 비접촉 자동 출결 시스템 Kidsly를 개발했습니다. 인식 모델 개발과 학습 데이터 수집·관리, 영상 데이터 전처리 파이프라인을 담당했고 FastAPI 기반 백엔드도 함께 다뤘습니다. 인턴이었지만 일정·태스크 관리를 포함한 프로젝트 관리 전반을 맡았습니다.
```

## 8. 링크

| 필드 | 값 |
| --- | --- |
| 링크명 | `포트폴리오 · 웹 이력서` |
| URL | `https://marinkim.xyz` |

## 9. 저장 확인

전부 붙여넣은 뒤 **새로고침** → 소개가 `LLM 기능을 제품에 붙이는 일보다`로 시작하는지,
메디솔브 설명에 `[Agent Workflow]`가 있는지 확인.
