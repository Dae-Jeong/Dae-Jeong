---
type: working
title: 케이스 서술 초안 — 5단 (문제·검토·결정·시스템·결과)
description: 포트폴리오 케이스별로 5단 서술을 evidence 기준으로 채운 작업본. 확정·확인 필요·생략을 구분한다.
timestamp: 2026-08-13
derived_from: [products/site/2026-08-13-case-narrative-research.md, evidence/projects/, evidence/claims/]
tags: [portfolio, narrative, working]
---

# 케이스 서술 초안 — 5단

`문제 → 검토 → 결정 → 시스템 → 결과` (PAAR).
`검토`와 `한계·다음 수`는 **선택 섹션**이다 — 근거가 없으면 생략한다 (§6·§7).

**표기**: ✅ evidence 확인됨 / ⚠️ 확인 필요(사용자 입력 대기) / ❌ 근거 없음 → 생략

## 채움 판정 요약

| 케이스 | 검토 | 한계·다음 수 | 비고 |
| --- | --- | --- | --- |
| AI 콘텐츠 생성 backend 재구축 | ✅ | ⚠️ | 판단 근거 2건 (재구축 · judge 도입) |
| 주문·재고 비동기 처리 | ✅ | ⚠️ | 메멘토 학습 전이 |
| 조직 표준 백엔드 템플릿 | ✅ | ⚠️ | 조직 맥락이 곧 판단 근거 |
| 실시간 상담 AI 세션 | ❌ | ⚠️ | code-backed 사실만, 판단 기록 없음 |
| 제품 운영 게이트 | ❌ | ⚠️ | 〃 |

---

## 01. AI 콘텐츠 생성 backend 재구축

### 검토 ✅

evidence: `thready.md:40` (User-confirmed 2026-07-19)

```
· 부분 수정 — 증상만 잡으면 의존성 구조가 그대로 남는다고 봤다. QA 티켓을 닫아도
  같은 영역에서 다른 형태로 재발하던 패턴이 그 근거였다. → 기각
· 전면 교체(FE 포함) — blast radius가 필요 이상으로 커진다. → 기각
· backend만 분리 교체 — FE(Next.js)는 유지. AI 모듈 확장이 예정돼 있어 FastAPI 분리가
  이후 확장 비용을 낮춘다고 봤다. 서비스 규모가 작은 시점이라 지금이 가장 싸다. → 채택
```

기각 근거를 함께 쓴다. `"돌아가는 기능을 왜 다시 만드나"`라는 사내 반대를
문제 누적·AI 모듈 확장·하네스 기반 이관 속도로 설득한 것도 이 칸에 들어간다.

### 검토 ✅ — 품질 판정(별도 축)

evidence: `thready.md:55` (User-confirmed 2026-07-19)

```
· 감으로 판정 — "좋은 글"의 기준 자체가 부재했다. 대표의 성공 사례도 본인 계정 n=1이라
  일반화할 수 없었다. → 기각
· LLM judge를 품질 게이트로 — 기준이 없는 상태에서 게이트를 세우면 무엇을 막는지 알 수 없다. → 기각
· LLM judge를 판단 데이터 축적 장치로 — 가설을 정량 판단으로 바꿔 데이터를 쌓고
  기준을 증명해가는 루프로 설계. → 채택
```

⚠️ **이게 이 케이스에서 가장 강한 판단이다.** judge를 "채점기"가 아니라
"기준을 발견하는 장치"로 쓴 것 — 지금 문안 어디에도 없다.

### 한계·다음 수 ⚠️

후보 (사용자 확인 필요):
- 3층 판정의 사람 판정 층은 여전히 사람 시간이 든다 — 어디까지 자동화할지 미결
- 실측 코퍼스는 "통계가 아니라 경향" — 표본 편향(검색 노출·주제·수집 방식)이 남아 있다
- **질문**: 다시 한다면 무엇을 바꾸겠나? 재구축 시점? 하네스 범위? judge 설계?

---

## 02. 주문·재고 비동기 처리

### 검토 ✅

evidence: `centurion.md` BAY Async (User-confirmed 2026-07-19 인터뷰)

```
· 실패 가능한 작업을 API 요청 안에서 처리 — 외부 알림·재고 연동의 지연과 실패가
  사용자 응답 경계까지 전파된다. → 기각
· 장애가 난 뒤에 분리 — 직전 회사에서 결제 실패의 불일치(선결제 예약 실패 롤백, 환불 처리
  순서, 환불 시 티켓 제거 시점)를 직접 수습해봤다. 같은 종류의 실패가 주문·재고에서도
  생긴다고 봤고, 사후 수습 비용이 훨씬 크다는 걸 경험했다. → 기각
· 제품 시작 시점부터 API 경계 밖 worker로 분리 + 재고 차감에 retry → 채택
```

**이 케이스의 핵심은 "겪어본 실패를 다음 제품에서 미리 막았다"는 것**이다.
두 회사를 잇는 서사이므로 반드시 남긴다.

### 한계·다음 수 ⚠️

- 전환(Celery → TaskIQ) 자체는 확인됐으나 **성능·지연 개선 정량은 측정값이 없다**
  (`centurion.md` Measurement boundary). 결과 칸에서 이 한계를 먼저 밝히는 게 낫다.
- **질문**: worker 분리 후 실제로 막힌 사고가 있었나? 있으면 그게 결과 칸의 강한 근거다.

---

## 03. 조직 표준 백엔드 템플릿

### 검토 ✅

evidence: `be-template.md` (User-confirmed 2026-07-19 인터뷰)

```
· 제품마다 각자 구조로 — 소수 백엔드 인원이 다수 제품을 담당하는 체제에서는
  제품 간 이동 비용이 그대로 병목이 된다. → 기각
· 문서 가이드만 배포 — 규약이 코드에 강제되지 않으면 제품마다 갈라진다. → 기각
· 실행 가능한 표준 템플릿 + agent context 내장 → 채택
```

✅ **공개 규모 표현 확정 (2026-08-13)**: 정확한 내부 제품 수는 공개하지 않고
**"10명 안팎의 엔지니어 조직에서 2~3명의 백엔드 엔지니어가 다수 제품을 담당"**까지 쓴다.

### 결과 — 근거 3종 ✅ (현재 문안에 일부만 반영)

evidence의 효과 3종:
1. 어떤 프로젝트든 **제품 정책만 파악하면 대응 가능** (컨텍스트 전환 비용 최소화)
2. logging·모니터링 등 **횡단 관심사를 전 제품에 일괄 반영**
3. 온보딩·FE 엔지니어도 패턴·규약·하네스 아래에서 **BE 로직 구현 가능**

→ ②③이 현재 케이스 문안에 없다. 특히 ③은 "표준이 사람을 확장한다"는 강한 근거다.

### 한계·다음 수 ⚠️

- **질문**: 템플릿이 안 맞아서 예외를 둔 제품이 있었나? 있으면 그게 정직한 한계다.

---

## 04. 실시간 상담 AI 세션

### 검토 ❌ — 생략

`centurion.md` SAY Realtime은 **code-backed 사실만** 있고 판단 기록이 없다
(WebSocket runtime, provider lifecycle, zombie session cleanup, reconnect race,
translation/audio pipeline). 없는 판단을 지어내지 않는다.

⚠️ **강도 주의**: `centurion.say-realtime` = **co-led**. "공동 주 기여" 표현 유지, 상향 금지.
provider 실명 비공개.

### 한계·다음 수 ⚠️

- **질문**: zombie session·reconnect race를 다루면서 "이렇게 했어야 했다" 싶은 지점이 있었나?
  있으면 그게 검토 칸을 여는 열쇠가 된다.

---

## 05. 제품 운영 게이트

### 검토 ❌ — 생략

`mediness.md`는 source-backed 구조 사실(pipeline registry, decision log, SPEC,
Work Package, release gate, version cut)과 tool-backed 운영 기록뿐이다. 판단 기록 없음.

### 결과 — 이미 한계를 밝히고 있다 ✅

현재 문안이 *"상태 확인 시간의 정량 변화나 에이전트의 자율 의사결정은 주장하지 않는다 —
측정하지 않았다"*로 한계를 명시한다. **5단의 `한계·다음 수`가 이미 구현된 유일한 케이스**다.

### 한계·다음 수 ⚠️

- **질문**: 게이트를 만들고 나서 실제로 막힌 릴리스가 있었나? 그 사례가 있으면
  "구조를 만들었다"가 "구조가 작동했다"로 올라간다.

---

## 사용자 확인 — 판정 결과 (2026-08-13)

1. **judge 설계 판단** → ✅ **재구축 케이스 검토 칸에 넣는다** ("판단 재구축 케이스 좋아")
2. **be-template 조직 수치** → ❌ **넣지 않는다** ("굳이 넣지마") — 가드레일 유지 확정.
   공개 표현은 "소수 백엔드 인원이 다수 제품을 담당하는 체제"까지
3. **"다시 한다면"** → ⏸ 보류 ("일단 남겨") — `한계·다음 수` 칸은 구조만 두고 비워둔다
4. worker 분리가 막은 사고 → ⏸ 보류 ("냅둬")
5. 게이트가 막은 릴리스 → ⏸ 보류 ("냅둬")

날짜 확정 (같은 턴): 아이즈솔 ~2021.06 · 스튜디오랩 ~**2024.01** (canonical 정정) · 메멘토 2024.10~

---

## 케이스 유형 — 검토 칸이 비었다고 약한 게 아니다 (2026-08-13 user-confirmed)

> "안 쓰는 영역은 사실 내가 AI를 다루는 직군을 간다면 좀 이런걸 해본 경험이 있어!
> 라는걸로 나를 어필할 수 있을텐데 그치?"

맞다. **케이스마다 파는 것이 다르다.** 전부 판단 서사로 만들 필요가 없다.

| 유형 | 파는 것 | 앞에 오는 것 | 해당 케이스 |
| --- | --- | --- | --- |
| **판단 증명형** | 재현 가능성 — 같은 상황에서 또 이렇게 판단한다 | `검토`(기각한 대안) | 재구축 · 주문·재고 · 조직 표준 |
| **경험 범위형** | **다뤄봤다는 사실** — 이 영역이 낯설지 않다 | **무엇을 다뤘나**(기술 표면) | 실시간 상담 AI 세션 |
| **운영 구조형** | 구조를 만들고 굴려봤다 | 구조와 그 한계 | 제품 운영 게이트 |

### 경험 범위형이 왜 유효한가 — JD 근거

[JD 리포트](../jd/reports/2026-08-11-po-ai-agent-market.md)가 AI Engineer 스크리닝 항목으로 꼽은 것:

> **"lab-only가 아닌 production LLM 운영"** — JD 스크린 항목 그 자체

`실시간 상담 AI 세션`이 담고 있는 것:

- **WebSocket 기반 실시간 LLM 파이프라인** — 스트리밍·세션 상태를 실서비스에서 다룸
- **STT + LLM 연결** — 멀티모달 입력 처리
- **provider lifecycle 관리** — 외부 모델 제공자의 실패·전환을 코드로 다룸
- **zombie session 정리 · reconnect race 처리** — 실시간 시스템의 실패 모드를 겪고 처리
- translation·audio pipeline

→ 판단 서사가 없어도 **"이 영역을 production에서 돌려봤다"는 것만으로 스크리닝을 통과**한다.
대부분의 지원자가 못 내미는 표면이다.

### 그래서 서술을 어떻게 바꾸나

경험 범위형 케이스는 `검토`를 비우는 대신 **`시스템` 섹션을 앞으로** 끌어올리고,
**무엇을 다뤘는지의 범위**를 먼저 보여준다.

```
문제 → (검토 생략) → 결정 → 시스템 ← 여기가 주인공 → 결과
```

- 기술 표면을 **나열이 아니라 실패 모드와 함께** 쓴다.
  "zombie session 정리"보다 "연결이 끊긴 세션이 서버에 남아 자원을 물고 있는 문제를 정리"가
  실제로 겪은 사람의 문장이다.
- **강도 주의**: `centurion.say-realtime` = **co-led**. "공동 주 기여" 유지, 상향 금지.
  provider 실명 비공개.

### 이력서 역량 축과의 연결

이 유형은 이력서 역량 축 `01 AI 제품 backend 구축·재구축`의 근거로도 쓰인다.
현재 이력서에는 실시간 AI 에이전트가 **`제품 운영·결정` 축의 마지막 줄**로만 들어가 있는데,
AI 직군 지원 시에는 **`01` 축이나 별도 축으로 올리는 것**이 맞다
([career-axes](../resume/career-axes.md)의 JD별 축 재배치).

→ **후속 검토**: AI Engineer 포지션 지원 시 축 순서에
`실시간·스트리밍 LLM 운영`을 별도로 세울지.
