# ── OS / editor ──────────────────────────────────────────────
.DS_Store
Thumbs.db
.idea/
*.swp

# ── Local agent/plugin installations ─────────────────────────
.agents/*
!.agents/skills/
.agents/skills/impeccable/
.impeccable/
skills-lock.json

# ── Tool runtime and render scratch ──────────────────────────
.playwright-mcp/
.pnpm-store/
.worktrees/
.tmp/
tmp/
.local/
tools/.visual-baseline/
graphify-out/

# Canonical wiki lives in the local LLM Wiki. No trailing slash: wiki is a symlink.
/wiki
/wiki.pre-ssot-20260910/
# Local evidence, application artifacts, projections, and review snapshots.
/output/

# ── Python (root tools + app/be + labs 서비스) ────────────────
.venv/
venv/
__pycache__/
*.py[cod]
.pytest_cache/
.ruff_cache/
.mypy_cache/

# ── Node / Next.js (app/fe + labs 서비스) ─────────────────────
node_modules/
.next/
out/
dist/
build/
.turbo/
*.tsbuildinfo
.vercel/
next-env.d.ts

# ── Secrets / env (전 층 공통 — example만 커밋 허용) ──────────
.env
.env.*
!.env.example
*.pem
*.key

# ── Infra (infra/ — 상태·자격증명은 로컬/원격 backend로만) ────
.terraform/
*.tfstate
*.tfstate.*
*.tfvars
!*.example.tfvars
crash.log
kubeconfig
*.kubeconfig
